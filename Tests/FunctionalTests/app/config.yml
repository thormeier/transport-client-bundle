framework:
    secret:     secret
    test:       ~
    validation: { enable_annotations: true }

# Replace actual Buzz Browser with instance of local mocked one that just returns fixtures
# This is needed to omit the dependency on the external API which might be down at any given time
parameters:
    transportclient.api.url: "http://transport.opendata.ch/v1/"
    buzz.client.class: Thormeier\TransportClientBundle\Tests\FunctionalTests\TestUtils\Buzz\Client\FixtureClient
